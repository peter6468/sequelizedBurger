<div class="container">

    <div class="jumbotron" style="background-color: orange">
        <div class="center col-sm-12" id="top-bar">
            <h1 class="text-center">🍔<strong>EAT DA BURGER DA SEQUEL</strong>🍔</h1>
            <h2 class="text-center">
            <img src="assets/img/images.jpg" alt=i"image burger">
            </h2>
        </div>
    </div>



    <div class="row" class="task">
        <div class="col-sm-6">
            <br>
            <h2>Burgers Availabe to Eat</h2>

            <ul>
            <!--creating pg that loops thr/burger data + 4 each peice of burger data-->
            {{#each burger_data}} {{#unless this.devoured}}
                <li>
            <!--div class="col-xs-9 text-center"-->
                <!--<pre>{{this.id}}. {{this.burger_name}} {{this.burger_price}}</pre>-->
                {{this.id}}. 
                {{this.burger_name}} 
                ${{this.burger_price}}
            <!--/div-->
            <!--div class="col-xs-3 text-center"-->
                <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
                    <input type="hidden" name="burger_id" type="text" value={{this.id}}>
                    <button type="submit" class="btn btn-default">
                        DEVOUR IT!!!
                    </button>
                </form>

            </li>
            {{/unless}} 
            {{/each}}
            </ul>
        </div>
    

    <div class="col-sm-6 text" class="task">
        <br>
        <h2>Burgers Devoured!!!</h2>
        {{#each burger_data}} 
            {{#if this.devoured}}
                <li>
                    {{this.id}}.
                    {{this.burger_name}}
                    ${{this.burger_price}}
                </li>
            {{/if}} 
        {{/each}}